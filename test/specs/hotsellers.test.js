// Grid will have items appear

// Hot Sellers items have 
  // sizes
    // size class: swatch-attribute-size
  // colors
    // color class: swatch-attribute-color
  // rating
    // class: rating-result
  // link to reviews
  // onHover will be highlighted

// Navigate to Homepage ✔️
  //   beforeAll(() => {
  //     homepage.goToURL("https://magento.softwaretestingboard.com");
  //   });
// Scroll to Content grid 
  // let element = driver.findElement(By.className('footer'));
  // driver.executeScript("arguments[0].scrollIntoView(true);", element);
// Verify that the grid is visible
  // boolean t = driver.findElement(By.name("txt-val")).isDisplayed();
// We need to create that something is true in testutils.js
